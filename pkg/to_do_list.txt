
2.0-0

    o  mboost-package (BH) + NEWS (BH) + startup message (TH)

    o  richer Family objects are nedded (e.g., prediction and extraction of
       coefs needs to be handled with care if family=Binomial())
       scale parameter NBinomial, PropOdds, Weibull 
                       (-> Docu + Examples MS)
       linkinv -> predict (type = "response") (TH)
       weighted median for offset in Huber / Laplace, 
       see Hmisc:::wtd.quantile (TH)

    o  predict.glmboost via coef.glmboost. 
       Remove special code in bolscw (TH)

    o  do we need Matrix on attach? (TH)

    o  add missing files to svn (in inst)  (TH)

future

    o  new base-learners (by example, brad, bmrf)

    o  check status == "N" in wpbc

    o  na.action defaults checken

    o  use better colours

    o  use col = rgb(0, 0, 0, 0.1) for lines in plot.cvrisk

    o  AIC(..., "classical") as default???

    o  multivariate responses

    o  reweight generic?

    o  hatvalues + AIC geht so nur fuer GaussReg!
    o  hat matrices fuer GAMS (Hastie & Tib)

    o  AIC(..., "corrected") monoton steigend???

    o  need party?

    o  look at coffee dataset
       http://sensor.ing.unibs.it/_people/pardo/dataset.html

    o  checks for gMLD
